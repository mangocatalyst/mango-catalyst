/** Staged placeholder: the Contact page agent owns this file. */
export default function ContactPage() {
  return (
    <main className="mx-auto w-full max-w-6xl flex-1 px-6 py-20 sm:px-10 sm:py-28">
      <div aria-hidden className="h-[3px] w-10 bg-amber" />
      <h1
        id="book"
        className="mt-6 max-w-[24ch] font-display text-[clamp(2.4rem,1.6rem+3vw,4rem)] font-bold uppercase leading-[1.02] tracking-[0.015em] text-ink"
      >
        Book a 15-minute fit call
      </h1>
      <p className="mt-5 max-w-[44rem] leading-[1.65] text-body">
        Full page on the way.
      </p>
    </main>
  );
}
